version: 2
updates:
  # Mantener actualizadas las acciones del workflow (canal de parches
  # de seguridad más común en pipelines de GitHub Actions)
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci: "
    open-pull-requests-limit: 5
